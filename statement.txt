In this project we used AI for the original proto files to generate method stubs in phase 1. We also used it to convert our demos into bash scripts that could be run through the terminal to automate testing. 